"""Concrete TAP vendor adapters (service-grade).

Adapters here subclass ``pancake_services.tap.adapter_base.TAPAdapter`` and are
registered by class path from the vendor config YAML (see dpi-demo/tap_vendors.yaml).
"""
